import { ScreenShell } from '@/components/ScreenShell';

export default function SettingsScreen() {
  return (
    <ScreenShell title="Settings" message="Theme and offline preferences are stored locally." />
  );
}
