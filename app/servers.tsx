import { ScreenShell } from '@/components/ScreenShell';

export default function ServersScreen() {
  return (
    <ScreenShell
      title="Servers"
      message="Saved servers will appear here. Connections require a native development build."
    />
  );
}
