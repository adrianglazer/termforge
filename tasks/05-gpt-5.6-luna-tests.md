# 05 — GPT-5.6 Luna: regression coverage and documentation

Validation/build policy: follow `tasks/README.md`. It supersedes older per-task device gates, repeated EAS/TestFlight requirements, and conflicting validation cadence in referenced plans. Historical results below remain evidence only.
Prerequisite: task 04 complete. Read `docs/implementation-plan.md` testing strategy and `instructions.md` §§47–50, 85–88. Execute only this file. Follow existing architecture and security policies; do not change them to make tests pass.

- Fill meaningful unit-test gaps: validation, key metadata, migrations, configuration import/export, pane trees, dimensions, settings, snippets, connection transitions, trust decisions, and redaction.
- Extend existing integration harnesses for failed/encrypted-key authentication, changed host keys, reconnect limits, disconnect/timeouts, SFTP operations/interrupted transfers, forwarding, and jump hosts.
- Run all feasible regressions locally, using controlled native adapters for component/controller tests and real implementations for integration assertions. Add UI regression flows for onboarding, server/key management, connection, pane splitting, restoration, terminal search, SFTP, and editor save/discard.
- Add fixtures/assertions for malformed escape sequences, Unicode/long lines, large paste/output, SFTP traversal, secret exclusion, and mock isolation. Generate temporary credentials; commit no secrets.
- Execute reproducible local performance scenarios with bounded-buffer, event-count, concurrency, and integrity assertions; reserve native rendering, memory, and touch responsiveness for one combined device workload. Prepare scenarios for 100,000 lines, rapid output, streaming logs, concurrent sessions, 4-/8-pane layouts, and large simultaneous transfers.
- Make only routine test, typing, lint, or documentation fixes. Record native, security, integration, and performance defects with minimal reproductions for task 06; do not weaken assertions.
- Update `docs/testing.md` and `docs/troubleshooting.md` with concise commands, EAS build IDs, TestFlight build numbers, device matrix, and actual results.

Done: checks are reproducible and runnable results recorded; device-only/unavailable checks remain explicitly pending. Record unresolved failures here for task 06. This is a validation handoff, not a declaration of release readiness.
