# 03 — GPT-5.6 Sol: native terminal, SSH, and services

Prerequisite: task 02 complete. Read the architecture/security documents and relevant terminal, SSH, SFTP, forwarding, and acceptance sections of `instructions.md`. Execute only this file. Verify uncertain APIs against current official documentation; preserve the approved contracts.

- Create a disposable Docker OpenSSH environment, generated untracked test credentials, file/terminal fixtures, and start/stop scripts. Include a bastion and forwarding targets where needed.
- Build Swift Expo Modules for Keychain, terminal, SSH, and SFTP through EAS Build. Prove library interoperability in a minimal internal development build on a registered physical iPhone before expanding implementation. Repeat signed acceptance through TestFlight once the minimal native path works. Record EAS build IDs/images, TestFlight build numbers, iPhone/OS, and actual results; stop dependent work if the proof fails or cannot be performed. Local Xcode is optional; iPad testing is optional and non-blocking.
- Implement secure key generation/import, encrypted keys, public-key export/fingerprints, deletion, biometric/auto-lock policy, and opaque references. Support approved algorithms without custom cryptography.
- Integrate the real emulator: ANSI/VT/xterm behavior, Unicode/wide characters, alternate buffers, bounded scrollback, resize, selection, clipboard, search, themes, title/bell events, safe OSC, keyboard modes, and efficient native data flow.
- Implement SSH authentication, first-use fingerprint confirmation, mismatch blocking, PTY I/O/resize, typed lifecycle states, errors, timeouts, keepalive, bounded backoff, concurrent sessions, and honest suspension/foreground recovery. Connections must outlive React rerenders.
- Implement SFTP operations and transfers with progress/cancellation/retry, local file access boundaries, path validation, and safe editor write-back. Implement local/remote forwarding and jump hosts; enable SOCKS only with verified support. Expose unsupported capabilities honestly.
- Integrate a minimal real terminal route in the existing shell. Keep mocks out of production paths.
- Run native/integration checks for trust, authentication, key protection, shell I/O, reconnect, SFTP, forwarding, and bastions. Exercise `instructions.md` §85 terminal commands, resize, Ctrl keys, Unicode, and high output on devices.

Done: real native services are usable through typed adapters; development build and applicable checks pass. Update architecture documents with verified capabilities, limitations, and short results; record completion/blockers here. Never label unavailable device/build checks as passed.

## Execution record — 2026-09-16

Status: **in progress; Keychain and pinned-library device gates passed, real terminal/SSH/SFTP acceptance remains.**

- Completed the independent fixture work: `tests/ssh-server/` contains a
  disposable Docker OpenSSH image, separate target and bastion services,
  generated untracked Ed25519 and encrypted-Ed25519 credentials, password and
  file fixtures, PTY tools, internal SFTP, a loopback forwarding target, and
  disposable host-key volumes. Start/stop scripts live under `scripts/`.
- Host-verified both healthy nodes, public-key SSH, `seq 1 100000`, SFTP
  download with byte comparison, local forwarding, and target access through
  the bastion. This validates the test environment, not the app.
- The current host is Linux, which is supported because iOS compilation is assigned to EAS. EAS project ownership, Apple signing, and a registered physical iPhone are now available. Baseline build `f88f0e63-633e-4089-9556-f0628fa80b50` compiled all four Expo module targets. Installation/launch and SwiftTerm/Citadel/Keychain interoperability remain unverified.
- On 2026-09-16, the user completed all six protected-key acceptance steps on
  the registered physical iPhone: protected Ed25519 generation, opaque
  reference/fingerprint presentation, authenticated protected use, and
  authenticated deletion all passed on an iPhone 13 running iOS 26.6.1.
  This passes the Keychain slice only; it does not pass the
  terminal/SSH/SFTP interoperability gate.
- Development build `8dc641fc-ceb2-47b6-862e-50cc92959a73` fixed the EAS
  archive ignore rule. Post-build inspection of the signed IPA confirmed that
  `TermforgeSecureStorage`, `TermforgeTerminal`, `TermforgeSsh`, and
  `TermforgeSftp` classes are embedded.
- Native package gate build `2d3e37b1-38f3-4454-87bc-04819ee39076` resolved
  SwiftTerm 1.19.0, Citadel 0.12.1, and the transitive Swift package graph, then
  stopped at Xcode's interactive SwiftTerm build-plugin trust validation. It is
  recorded as failed, not passed. The next build uses an EAS-only trust-dialog
  bypass while retaining exact repository/version pins.
- Retry `38c28428-6d7f-47a3-bf8e-5faa5c0de7db` passed plugin validation and
  compiled the resolved package graph, then failed on Swift 6's explicit import
  visibility rule in the inline probe (`ExpoModulesCore` was implicit while the
  generated provider imports it as `internal`). It is recorded as failed; the
  probe now uses matching internal imports and internal class visibility.
- Signed development build `7e38f362-631d-4ec5-902d-24988ed31ceb`
  finished. Its IPA was downloaded and inspected: the app binary embeds
  `TermforgeNativeProbeModule` and the pinned `SwiftTerm 1.19.0` /
  `Citadel 0.12.1` labels. Installation, launch, and the on-device “Verify
  native packages” result remain pending and are not marked passed.
- The user installed that build on the registered physical iPhone and confirmed
  the on-device native package probe passed, loading SwiftTerm 1.19.0 and
  Citadel 0.12.1 together on an iPhone 13 running iOS 26.6.1. This passes the
  pinned-library interoperability gate and permits
  dependent native implementation; it does not yet pass real PTY/SSH/SFTP
  acceptance.
- Real-terminal development build `3927edad-f786-4955-8974-0457e88f6951`
  reached the application Swift sources, then failed on three facade compiler
  errors: cross-file `fileprivate` access, Citadel's authentication closure
  syntax, and an ambiguous `Task` initializer. It is recorded as failed. Those
  exact errors were corrected before retry
  `abbfd424-b9ae-4791-9157-b5e9dc75e544`.
- Retry `abbfd424-b9ae-4791-9157-b5e9dc75e544` passed those three sites and
  failed at the NIOSSH fork's explicit terminal-mode mapping initializer. It is
  recorded as failed; the PTY request now uses `SSHTerminalModes([:])`.
- Signed development build `74ecc3e0-ebeb-4184-ab96-a3a3701c0d65` finished
  with the real native host-key inspection, pinned-host password connection,
  PTY, and SwiftTerm view. Installation and real SSH/terminal behavior on the
  iPhone remain pending and are not marked passed.
- The user installed that build on an iPhone 13 / iOS 26.6.1 and successfully
  completed first-use host approval, password authentication, PTY connection,
  command entry, and output. Two defects were found: the software keyboard
  obscured form/terminal content, and the disposable SSH server launched a
  POSIX rather than UTF-8 locale. Keyboard-aware layouts and a `C.UTF-8`
  fixture environment were added; their device retest remains pending.
- Per the explicit “stop dependent work” rule, no unproven native module or
  production adapter was added, the mock was not wired into production, and the
  terminal route continues to state that it is disconnected. Terminal, SSH,
  SFTP, forwarding, jump-host app behavior, and section 85 device acceptance
  remain **unverified**, not passed.

Resume from this Linux host using the configured EAS project, saved signing, and registered iPhone. First install and verify the baseline, then cloud-build the minimal pinned native facade and record the proof fields in `docs/terminal-engine.md`; expand the native implementation
only after all six proof-gate items pass. Then upload a store-distribution build
for TestFlight acceptance; public App Store review remains out of scope. No iPad result is required.
