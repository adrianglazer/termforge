# 04 — GPT-5.6 Terra: workstation features

Validation/build policy: follow `tasks/README.md`. It supersedes older per-task device gates, repeated EAS/TestFlight requirements, and conflicting validation cadence in referenced plans. Historical results below remain evidence only.
Prerequisite: task 03's recorded native proof and usable service contracts; milestone-only device checks may remain pending. Read its verified capabilities, architecture documents, and `instructions.md`. Execute only this file. Use existing native services; do not redesign security or simulate missing capabilities.

- Complete server dashboard/editor: validation, authentication references, connection options, duplicate/delete/connect, status, and SFTP entry points.
- Add SSH-key management and known-host screens using native protection and trust flows; include generation/import, public-key copy/export, fingerprints, rename/delete, and server usage.
- Integrate the central session manager with workspace tabs and recursive panes: split, resize, focus, close, zoom/restore, rename, duplicate, and navigation. Persist layouts/settings/configuration without pretending disconnected sessions survived relaunch.
- Build terminal selection/copy/paste/share/search controls, font/cursor/scrollback preferences, customizable accessory keys, modifiers, and function keys. Connect pane sizing to remote PTY resize. External-keyboard behavior is optional and non-blocking.
- Build local/remote SFTP browsing, sorting/search, file information/permissions, directory/file actions, user-selected transfers, progress/cancellation/retry, and interruption errors. Add a lightweight editor with line numbers, search/replace, save/write-back, dirty-state protection, and discard.
- Add validated forwarding/jump-host controls for supported native capabilities. Implement snippets with categories/favorites/variables and explicit execution, command palette, and optional large-paste warnings.
- Complete settings, non-secret import/export, connection history, onboarding, and clearly labeled demo mode. Ship Default Dark/Light, Solarized Dark/Light, Dracula-style, and Monokai-style themes with verified license obligations.
- Implement adaptive iPhone layouts in portrait and landscape, VoiceOver labels, Dynamic Type outside the terminal grid, contrast, and reduced motion. iPad, pointer, and hardware-keyboard layouts are optional and non-blocking.

Done: real end-to-end workflows work, including four independent sessions, workspace restoration, key management, and SFTP editing. Run local feature/controller/component tests, lint, and TypeScript. Verify four-session isolation, workspace restore, key-management state, and SFTP editor flows with controlled adapters; track real native integration separately. Defer only physical/iOS-specific acceptance to the shared candidate; no task-specific EAS or TestFlight build. Update related docs and record EAS build/device evidence and concise blockers. Escalate native defects to task 03's scope before declaring completion.
