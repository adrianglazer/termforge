# 02 — GPT-5.6 Terra: foundation and persistence

Prerequisite: task 01 complete. Read `instructions.md`, `docs/implementation-plan.md`, and task 01's architecture documents. Execute only this file; preserve approved architecture and existing work.

- Scaffold the approved Expo development-build project with strict TypeScript, navigation shell, lint/format/test commands, package lockfile, and isolated native-module directories. Configure EAS development/preview/production profiles and a clearly replaceable bundle identifier.
- Implement shared types, validation, application error/logging contracts, repository interfaces, and SQLite migrations for servers, key metadata, settings, snippets, workspaces, known hosts, and non-sensitive connection history.
- Implement recursive pane-tree operations and validated non-secret configuration import/export. Keep credentials out of persistence, exports, logs, and ordinary state.
- Add minimal Servers, Terminal, and Settings shells, theme tokens, loading/error boundaries, and offline startup. Keep a clearly labeled, isolated `MockTerminalEngine` for development; never report a real connection from a mock.
- Prepare typed native adapters and session-manager interfaces from task 01. Defer feature UI and real native behavior until task 03's device proof.
- Test validation, migrations, pane-tree operations, and secret exclusion. Document installation, Linux development workflow, EAS cloud compilation, internal-device distribution, and TestFlight prerequisites in `docs/development.md` and `docs/eas.md`.

Done: clean installation, tests, lint, and TypeScript checks pass; metadata survives relaunch offline. Verify the development build when credentials/tooling permit; record unavailable checks explicitly. Update this file with concise results/blockers. Do not proceed with known failures.

## Results (2026-09-16)

- Scaffolded SDK 57 Expo development-build project with strict TypeScript, Expo Router shell, EAS profiles, isolated native-module directories, migrations, contracts, validation, pane-tree editing, safe exports, and development documentation.
- Verified: dependency installation produced `package-lock.json`; `npm run lint`, `npm run format`, `npm run typecheck`, and `npm test` pass (5 unit tests). The local host is Node 20.6.1, below the project’s Node 22.23.1 minimum, so installation emitted engine warnings but checks completed.
- Unavailable: iOS development-build/device/offline-relaunch verification requires EAS project ownership, Apple signing, and registered physical devices. Local Xcode is not required. No real connection behavior is claimed; the development mock always reports disconnected.
